using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GrabController : MonoBehaviour
{
    /*
    public HandsController handsController;
    public GameObject handsCenter;
    public GameObject redCube;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Ra

        if (handsController.grabEnabled == true)
        {
            if (redCube.name == "RedCube")
            {


                Vector3 gmPos = handsCenter.gameObject.transform.position;
                //collision.gameObject.transform.position = gmPos;
                redCube.GetComponent<Rigidbody>().AddForce(transform.forward * 20);

                //collision.rigidbody.useGravity = false;
                //collision.rigidbody.isKinematic = true;
                Debug.Log("grab");

            }
            
        }
    }
    private void OnCollisionEnter(Collision collision)
    {
        
        
    }
    */
    
}
